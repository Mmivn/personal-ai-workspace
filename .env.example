# Copy to .env and fill in real values. .env is gitignored — never commit real secrets.
# Add one entry here per env var a tool introduces, with a placeholder value.

# ANTHROPIC_API_KEY=sk-ant-...

# tools/zodiac_numerology — AI provider chain (Gemini -> Groq -> Mistral ->
# Cloudflare Workers AI -> OpenAI last resort), read by ALL_API's
# Config.from_env() via tools/zodiac_numerology/ai_service.py. On Streamlit
# Community Cloud these are set as that app's own Secrets instead of .env —
# see tools/zodiac_numerology/README.md.
# GEMINI_API_KEY=
# GROQ_API_KEY=
# MISTRAL_API_KEY=
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=
# OPENAI_API_KEY=
